/// Base abstract class for all repositories.
/// Provides common interface for data access with Either<Failure, Data> for error handling.
abstract class BaseRepository {
  // Common methods if any, e.g. check connectivity
}