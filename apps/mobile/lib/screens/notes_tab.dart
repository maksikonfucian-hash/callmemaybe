import 'package:flutter/material.dart';

/// Placeholder for Notes tab.
class NotesTab extends StatelessWidget {
  const NotesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Notes Tab - Placeholder'),
    );
  }
}